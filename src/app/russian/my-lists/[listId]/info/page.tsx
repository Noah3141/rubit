import Content from "./content";
import { useVocabularyList } from "~/layouts/VocabListSuite/context";

const ListInfoPage = async ({}: { params: { listId: string } }) => {
    return <Content />;
};

export default ListInfoPage;
