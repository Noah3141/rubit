import classNames from "classnames";
import styles from "./index.module.css";

import React from "react";

const Option = () => {
    return <div className={classNames(styles.option)}>Option</div>;
};

export default Option;
